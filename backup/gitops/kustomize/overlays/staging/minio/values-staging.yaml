# Staging environment overrides for MinIO Helm chart
namespace: staging

replicas: 1

persistence:
  size: 20Gi

resources:
  requests:
    cpu: 100m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 1Gi

