# Production environment overrides for MinIO Helm chart
namespace: minio

replicas: 2

persistence:
  size: 50Gi

resources:
  requests:
    cpu: 200m
    memory: 1Gi
  limits:
    cpu: 1000m
    memory: 2Gi

