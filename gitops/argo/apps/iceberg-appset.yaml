apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: iceberg
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - environment: dev
            namespace: iceberg
            valuesFile: gitops/kustomize/overlays/dev/iceberg/values-dev.yaml
            prune: true
          - environment: staging
            namespace: iceberg
            valuesFile: gitops/kustomize/overlays/staging/iceberg/values-staging.yaml
            prune: true
          - environment: prod
            namespace: iceberg
            valuesFile: gitops/kustomize/overlays/prod/iceberg/values-prod.yaml
            prune: false
  template:
    metadata:
      name: iceberg-{{environment}}
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: multipass
      source:
        repoURL: https://github.com/your-org/multipass-gitops.git
        targetRevision: main
        path: gitops/helm/iceberg
        helm:
          valueFiles:
            - values.yaml
            - "{{valuesFile}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{namespace}}"
      syncPolicy:
        automated:
          prune: {{prune}}
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m

